# KodeForge — Production dependencies
# Source of truth: pyproject.toml [project.dependencies]
# Mirrored here for users who prefer pip install over pip install -e ".[dev]"
# Last synced: 2026-07-01 with pyproject.toml v0.4.0.dev0

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
pyyaml>=6.0
sse-starlette>=1.8.0
numpy>=1.24.0
